services:
  build_front:
    image: node:22-alpine
    working_dir: /app

    volumes:
      - ./app:/app
      - node_modules:/app/node_modules
    command: sh -c "npm install && npm run build"


volumes:
  node_modules: